@using NewLife.Common;
@using NewLife.Cube.WebMiddleware;
@using NewLife.Cube.Charts;
@{
    var set = NewLife.Cube.Setting.Current;
    var sys = SysConfig.Current;

    var title = ViewBag.Title + "";
    if (title != "" && !title.Contains(" - ")) title += " - " + sys.DisplayName;
}
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="新生命团队,NewLife,魔方,Cube,XCode" />
    <title>@title</title>
    <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon">

    <!-- 页面内联样式 -->
    @RenderSection("css", required: false)

    <link rel="stylesheet" href="/yuque/lake-content-v1.css">
</head>
<body>
    <header>
        @await Html.PartialAsync("_Header_yq")
    </header>

    @RenderBody()

    <footer class="footer">
        @await Html.PartialAsync("_Footer_yq")
    </footer>

    @RenderSection("scripts", required: false)
</body>
</html>